<?php

/**
 * Конфигурация подключаемого модуля синхронизации с 1С.
 * Все флаги вынесены сюда, чтобы поведение можно было менять без правок кода.
 */
return [
    /**
     * Глобальный рубильник. Если выключить, модуль автоматически подменяется
     * на null-реализации, а приложение продолжает работать в старом режиме.
     */
    'enabled' => env('ONEC_SYNC_ENABLED', true),

    'cells' => [
        /**
         * Нужно ли автоматически удалять локальную заявку, когда слоту из 1С
         * приходит статус free (и при этом указан claim_id нашей записи).
         */
        'auto_delete_on_free' => env('ONEC_SYNC_AUTO_DELETE', true),
    ],
];
